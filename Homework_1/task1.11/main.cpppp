#include <iostream>

using namespace std;

int main()
{
    int x = 0;
    cout << "Enter x to calculate the x^4 + x^3 + x^2 + x + 1 : ";
    cin >> x;
    int squareX = x * x;
    cout << "Result is : " << squareX * (squareX + x) + squareX + x + 1;
}