ищу лишнию память в update()
память, которую надо удалять вручную - это память, которая выделяется через new
поэтому выписываю все new и пишу в скобках где они удаляются
ничинаем в модуле hashtable.cpp:
	34 строка table.table[] (удаляется в 51 строке модуля hashtable.cpp)
	34 строка каждый элемент списков table.table[i](удаляется в строке 57 модуля list.cpp)
	34 строка каждое поле value элементов списков table.table[i](удаляется в строке 56 модуля list.cpp(точнее в строке 20 модуля string.cpp))
	37 строка newTable.table[] (удаляется в 38 строке модуля hashtable.cpp)
	40 cтрока newTable.table[] (удалять не нужно, т.к. это новая хэш таблица)
	на 46 строке проваливаемся в модуль list.cpp:
		на 54 строке проваливаемся в модуль string.cpp:
			29 строка newString.data[] (удаляется в строке 48 модуля hashtable.cpp(точнее в строке 20 модуля string.cpp))
	47 строка -> 58 -> 69 модуль list.cpp:
		28 или 39 newElement (удалять не нужно, т.к. это элемент списков в новой хэш таблице)
		30 или 41 проваливаемся в string.cpp:
			29 строка newString.data (удалять не нужно, т.к. это поле value в элементах списков новой хэш таблицы)
	

	
	